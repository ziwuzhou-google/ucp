{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ucp.dev/schemas/common/types/loyalty_membership.json",
  "title": "Loyalty Membership",
  "description": "Loyalty membership that the business offers or associated with.",
  "type": "object",
  "required": ["id", "name", "tracks"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique loyalty membership identifier."
    },
    "name": {
      "type": "string",
      "description": "Business specific name of the loyalty membership/program."
    },
    "member_id": {
      "type": "string",
      "description": "User's loyalty membership ID (e.g. loyalty card #, membership #)."
    },
    "tracks": {
      "type": "array",
      "items": {
        "$ref": "membership_track.json"
      },
      "description": "List of tracks associated with the loyalty membership."
    },
    "activated_tracks": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of tracks that the customer is activated for, identified by the `id` of the Membership Track object."
    },
    "rewards": {
      "type": "array",
      "items": {
        "$ref": "membership_reward.json"
      },
      "description": "List of quantifiable rewards value the user holds. Each object encapsulates one type of reward the membership offers."
    },
    "provisional": {
      "type": "boolean",
      "default": false,
      "description": "True if this membership requires additional verification."
    },
    "eligibility": {
      "$ref": "../../shopping/types/reverse_domain_name.json",
      "description": "The eligibility claim accepted by the Business for this membership. Corresponds to a value from context.eligibility."
    }
  }
}
